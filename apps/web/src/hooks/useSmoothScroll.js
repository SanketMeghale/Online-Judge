/**
 * Pure native browser scroll handler.
 * Preserves native browser inertia, two-finger touchpad swipe, and mobile momentum scrolling.
 */
export function useSmoothScroll() {
  // Rely on native browser scrolling with zero JS interception
}
