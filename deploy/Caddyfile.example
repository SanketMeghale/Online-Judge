# Replace this hostname with the DNS name pointing to the judge VM.
realtime.example.com {
    encode zstd gzip
    reverse_proxy 127.0.0.1:4001

    header {
        X-Content-Type-Options nosniff
        Referrer-Policy strict-origin-when-cross-origin
        -Server
    }
}
